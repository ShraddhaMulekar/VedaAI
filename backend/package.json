{
  "name": "vedaai",
  "version": "1.0.0",
  "private": true,
  "description": "Smart Answer Sheet Analysis & AI Evaluation",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix server",
    "dev": "concurrently -n frontend,server -c blue,green \"npm run dev --prefix frontend\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
